#cloud-config
users:
  - name: ${username}
    groups: sudo
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]
    shell: /bin/bash
    ssh-authorized-keys:
      - ${ssh_key}

package_update: true
package_upgrade: false
packages:
  - nginx
  - curl

runcmd:
  - echo "Hello ${username}!" > /etc/motd
